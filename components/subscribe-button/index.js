import './assets/subscribe-button.css';

const styles = civilCMSClassnames['subscribe-button'];

const subscribeButtonConfig = {
  name: 'subscribe-button',
  options: {
    styles,
  },
};

export default subscribeButtonConfig;
